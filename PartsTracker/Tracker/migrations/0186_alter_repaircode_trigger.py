# Generated by Django 5.1.6 on 2026-09-22 17:06

from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('Tracker', '0185_repaircode_rebuildscopepreset_and_more'),
    ]

    operations = [
        migrations.AlterField(
            model_name='repaircode',
            name='trigger',
            field=models.CharField(choices=[('RECONDITION', 'When the recovered component needs work before it goes back'), ('REPLACE_POOL', 'When the slot is filled from recovered stock'), ('REPLACE_BUY', 'When the slot is filled by a purchase'), ('REUSE', 'When the recovered component goes back as-is'), ('ALWAYS', 'Always — part of the base scope, whatever the finding'), ('PRESET', 'Only when a rebuild level includes it')], default='RECONDITION', help_text='Which finding raises this code. ALWAYS means it is part of the base scope and is not raised by a finding at all.', max_length=20),
        ),
    ]
